Memory allocation: Per frame, static, per module

Printf Verbosity, channel

Profiling:
	Func speed
	Watch_Func( void* ptr, string name ) -> Push, parent of following
	Unwatch = pop all

Module dependencies -> Name
	Pause thread when requires

GUI / Debug draw

GUI Modules
	menu
	hud
	gui

Physics module

Camera + Pixbuf

Jobs + Better event system + timers

Pure component based + Memory pools

Pointer fixup table / offsets / GUID / L(ocal)UID

Schemas 
